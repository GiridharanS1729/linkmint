services:
  postgres:
    image: postgres:16-alpine
    container_name: linkvio-postgres
    environment:
      POSTGRES_DB: linkvio
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: linkvio-redis
    ports:
      - '6379:6379'

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: linkvio-backend
    environment:
      PORT: 3000
      HOST: 0.0.0.0
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/linkvio
      REDIS_URL: redis://redis:6379
      JWT_SECRET: super-secret-change-me
      JWT_EXPIRES_IN: 1m
      FRONTEND_ORIGIN: http://localhost:5173
    depends_on:
      - postgres
      - redis
    ports:
      - '3000:3000'

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: linkvio-frontend
    environment:
      VITE_API_URL: http://localhost:3000
    depends_on:
      - backend
    ports:
      - '5173:5173'

volumes:
  postgres_data:
