# Docker assets

Use the root `docker-compose.yml` for local containers.

Services exposed:
- frontend: http://localhost:5173
- backend: http://localhost:3000
- postgres: localhost:5432
- redis: localhost:6379
